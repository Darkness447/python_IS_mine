# iterator in python

# consist of method __iter__() __next__()
# list, tuple,sets and dictonary all are iterable objects very important


# we can wrap all this with myit = iter() next(myit)

mystr = "banana"
myit = iter(mystr)

